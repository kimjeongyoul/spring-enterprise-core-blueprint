spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  
  jpa:
    hibernate:
      ddl-auto: validate # 상용에선 절대 자동 생성 금지
    show-sql: false

jwt:
  secret: ${JWT_SECRET}

# 상용 로깅 전략
logging:
  level:
    root: info
    com.vibe: info
